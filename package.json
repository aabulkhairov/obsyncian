{
  "name": "obsync",
  "version": "1.0.9",
  "description": "Affordable end-to-end encrypted vault sync for Obsidian",
  "main": "main.js",
  "scripts": {
    "dev": "node esbuild.config.mjs",
    "build": "tsc -noEmit -skipLibCheck && node esbuild.config.mjs production",
    "test": "vitest run",
    "publish:dev": "node version-bump.mjs && npm run build && mkdir -p ../server/public/dev-plugin && cp main.js manifest.json styles.css ../server/public/dev-plugin/",
    "version": "node version-bump.mjs && git add manifest.json versions.json"
  },
  "keywords": [
    "obsidian",
    "sync"
  ],
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^22.0.0",
    "esbuild": "^0.24.0",
    "obsidian": "latest",
    "tslib": "^2.8.0",
    "typescript": "^5.7.0",
    "vitest": "^2.1.0"
  }
}
